# rest-api

TaxMaxi REST API.

`@my/rest-api` owns API contracts and handlers. Source sync and replay entrypoints depend on `@my/sync-engine`; persistence is used for non-sync data access and supporting application services.
