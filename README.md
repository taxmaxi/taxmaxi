# TaxMaxi - The Solana tax API

TaxMaxi is an open-source crypto tax API. The API follows RESTful principles and its docs can be found under https://api.taxmaxi.com/openapi.json. Install the CLI with `npm i -g tax` to calculate crypto taxes right from your terminal: `tax <your wallet address>`. The first supported jurisdiction is Germany.
